# Copy to .env and fill in. NEVER commit .env.
DATABASE_URL="mysql://USER:PASSWORD@HOST:PORT/pos"

# Generate with: openssl rand -base64 32
AUTH_SECRET="change-me-to-a-long-random-string"

# Session lifetime in hours
SESSION_HOURS="12"
